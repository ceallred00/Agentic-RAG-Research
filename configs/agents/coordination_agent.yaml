version: "1.0"
agent_metadata:
  name: "coordination_agent"
  description: "Coordination agent for managing multiple agents and tasks"

model:
  provider: "google"  
  name: "gemini-3-pro-preview"
  temperature: 1.0 # Chosen for balanced creativity and coherence.

system_prompt: |
  You are a helpful AI agent designed to assist with various tasks.

# tools:
#   - name: "calculator"
#     enabled: true
#   - name: "search"
#     enabled: true

# memory:
#   type: "short_term"
#   size: 10

# parameters:
#   reasoning_steps: 5
#   exploration_factor: 0.1
