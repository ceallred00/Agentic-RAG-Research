version: "1.0"
agent_metadata:
  name: "rag_agent"
  description: "RAG agent for retrieval-augmented generation tasks"

model:
  provider: "google"
  name: "gemini-3-pro-preview"
  temperature: 0.3 # Chosen to favor accuracy and relevance.


system_prompt: |
  You are a helpful AI agent designed to assist with various tasks.

# tools: 
#   - name: "embed_query"
#     enabled: true
#   - name: "embed_search"
#     enabled: true

# memory:
#   type: "short_term"
#   size: 10

# parameters:
#   reasoning_steps: 5
#   exploration_factor: 0.1
